spring:
  application:
    name: INVENTORY-SERVICE

server:
  port: 8080

eureka:
  client:
    register-with-eureka: true   # ensures this service registers itself
    fetch-registry: true         # fetches other services from Eureka
    service-url:
      defaultZone: http://localhost:8761/eureka/
